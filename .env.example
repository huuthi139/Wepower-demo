# Google Apps Script Web App URL (server-side) - Legacy, optional
GOOGLE_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec

# Google Sheet ID - extracted from Google Sheets URL (for courses data)
GOOGLE_SHEET_ID=YOUR_GOOGLE_SHEET_ID

# Public Google Apps Script URL (client-side fallback)
NEXT_PUBLIC_GOOGLE_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec

# Public Google Sheet ID (client-side CSV fallback)
NEXT_PUBLIC_GOOGLE_SHEET_ID=YOUR_GOOGLE_SHEET_ID

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# JWT Secret for session management (minimum 32 characters)
JWT_SECRET=your-jwt-secret-minimum-32-characters-long

# Resend Email Service (https://resend.com)
RESEND_API_KEY=re_your_resend_api_key

# App URL (used for email links)
NEXT_PUBLIC_APP_URL=https://wepower.vn

# Sync Secret for Google Apps Script webhook (auto-sync Sheet → Supabase)
# Generate with: openssl rand -hex 32
SYNC_SECRET=your-sync-secret-minimum-32-characters
